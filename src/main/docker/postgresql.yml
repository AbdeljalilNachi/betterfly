version: '2'
services:
  betterfly-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/betterFly/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=betterFly
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
